# Comments after directives are handled

Host github.com
  User git                      # main git account
  IdentityFile ~/.ssh/id_%h        # key per host (skipped by rule due to %)
  ProxyCommand "ssh -W %h:%p jump"   # jump host

Host staging
  User admin             # admin user for staging
