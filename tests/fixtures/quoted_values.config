# Quoted values with spaces

Host work
  ProxyCommand "ssh -W %h:%p bastion.corp"
  User deploy_user

Host personal
  ProxyCommand "/usr/bin/ssh -o StrictHostKeyChecking=no tunnelhost"
  User personal_user
